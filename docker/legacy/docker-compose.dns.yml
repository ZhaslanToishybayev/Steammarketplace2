version: '3.8'

services:
  # Backend API - Add DNS servers
  backend:
    dns:
      - 8.8.8.8
      - 8.8.4.4
      - 1.1.1.1

  # Worker - Add DNS servers
  worker:
    dns:
      - 8.8.8.8
      - 8.8.4.4
      - 1.1.1.1

  # Frontend - Add DNS servers (for API calls)
  frontend:
    dns:
      - 8.8.8.8
      - 8.8.4.4
      - 1.1.1.1